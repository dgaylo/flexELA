if(ELA_USE_MPI)
else()
    set(TEST_PGRM output_test)
    add_executable(${TEST_PGRM} output_serial_test.cpp)
    target_link_libraries(${TEST_PGRM} GTest::gtest_main ${LIBRARY_NAME})
    gtest_discover_tests(${TEST_PGRM})
endif(ELA_USE_MPI)
